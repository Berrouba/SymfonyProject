{% extends 'base.html.twig' %}
  
   
{% block body %}
<center><h1 >List of best books</h1></center>
<div class="container">
<div class="row flex">
{% for p in books %}
<div class="col-3">
<div class="card">
<div class="card-body">
  Identity : {{ p.id}}<br>
  Title : {{ p.title}}<br>
  Price : {{ p.price}}<br>
  <a href="{{ path('book.show',{'id': p.id })}}">Voir Détails</a>
</div>
</div>
<br>
</div>

{% else %}   
  <h2> la table livre est vide </h2>
{% endfor %}
</div>
</div>

{% endblock%}